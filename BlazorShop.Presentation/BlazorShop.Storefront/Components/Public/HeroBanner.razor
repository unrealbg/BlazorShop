<section class="relative overflow-hidden bg-neutral-900 text-white">
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_left,_rgba(56,189,248,0.28),_transparent_45%),radial-gradient(circle_at_bottom_right,_rgba(245,158,11,0.28),_transparent_40%)]"></div>
    <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16 sm:py-20 lg:py-24 grid gap-10 lg:grid-cols-[1.2fr_0.8fr] lg:items-center">
        <div>
            <p class="text-sm font-semibold uppercase tracking-[0.3em] text-sky-200">Production SEO Storefront</p>
            <h1 class="mt-4 text-4xl sm:text-5xl lg:text-6xl font-black tracking-tight">Discover published products on real public routes.</h1>
            <p class="mt-6 max-w-2xl text-base sm:text-lg leading-8 text-neutral-200">
                BlazorShop now serves server-rendered category and product pages designed for production SEO, while the existing admin and interactive client remain available alongside the new public architecture.
            </p>
            <div class="mt-8 flex flex-wrap gap-3">
                <a href="@StorefrontRoutes.NewReleases" class="inline-flex items-center rounded-md bg-sky-400 px-4 py-2 text-sm font-semibold text-neutral-950 hover:bg-sky-300">Explore New Releases</a>
                <a href="@StorefrontRoutes.TodaysDeals" class="inline-flex items-center rounded-md border border-white/20 px-4 py-2 text-sm font-semibold text-white hover:bg-white/10">View Today's Deals</a>
            </div>
        </div>

        <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-1">
            <div class="rounded-3xl border border-white/10 bg-white/10 p-5 backdrop-blur-sm">
                <div class="text-xs font-semibold uppercase tracking-[0.2em] text-sky-200">Canonical Pages</div>
                <div class="mt-3 text-2xl font-extrabold">/category/{slug}</div>
                <p class="mt-2 text-sm text-neutral-200">Category discovery now lives on stable, server-rendered slug routes.</p>
            </div>
            <div class="rounded-3xl border border-white/10 bg-white/10 p-5 backdrop-blur-sm">
                <div class="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Product Pages</div>
                <div class="mt-3 text-2xl font-extrabold">/product/{slug}</div>
                <p class="mt-2 text-sm text-neutral-200">Product discovery is no longer trapped inside modal-only public flows.</p>
            </div>
        </div>
    </div>
</section>